export type ShoppingCardState = {
  isOpen: boolean
  toggleShoppingCard: () => void
}

export type LoginState = {
  isOpen: boolean
  toggleLogin: () => void
}

export type SignupState = {
  isOpen: boolean
  toggleSignup: () => void
}
